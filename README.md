# python_playwright_core
Requirment
1. python should be installed
2. run the following commands to install depedency

    1. pip install pytest
    2. pip install pytest-playwright
    3. playwright install
